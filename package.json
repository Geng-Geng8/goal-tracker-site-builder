{
  "name": "universal-goal-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check core.js && node --check app.js && node --check pwa.js && node --check sw.js"
  }
}
